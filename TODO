- Fit the benchmark more precisely to the article (exact same configuration and parameters)
	E.G. "For these tests, the benchmark runs a simulation of 50 clients sending 5 messages to themselves trough a network of 50 mixnodes and 50 authorities."
	|--> Then how many simulation are run ? Fixed path length (5) ? Reconstruction Threshold (20) ?